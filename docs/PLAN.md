# PLAN — dota-kill-clipper

Текущая фаза: **5. Shorts и YouTube** (код готов 2026-09-13, ждёт входа в YouTube и аудита Google).

## Фаза 0 — Интервью и спека
- [x] Установить скиллы из `My skill for claude code` (2026-09-09; см. MEMORY.md, что именно и как)
- [x] Разведка окружения: OBS 32.2.2, ffmpeg, Python 3.14, GSI-конфиги, код d2pt для переиспользования
- [x] Создать репозиторий и документы (README, CLAUDE.md, PROJECT, PLAN, MEMORY, SCENARIOS)
- [x] Продумать сценарии и краевые случаи → `SCENARIOS.md`
- [x] Получить ответы пользователя на вопросы из `SCENARIOS.md` (2026-09-09)
- [x] Обновить `PROJECT.md` до финальной спеки
- [x] Установить Python 3.12 для OBS (`py install 3.12`, дефолт 3.14 не тронут)
- [x] Создать публичный репозиторий `Betreazen/dota-kill-clipper` и запушить документы
- [x] Спека `PROJECT.md` утверждена; написан `docs/START_PROMPT.md` и команда `/implement` для сессии реализации
- [x] Старт фазы 1 в новой сессии (`/implement` в этом репозитории, 2026-09-09)

## Фаза 1 — Чистая логика, TDD (2026-09-09, 57 тестов зелёные на 3.14, импорт на 3.12 проверен)
- [x] Структура: пакет `killclipper/` + точка входа `obs_dota_kill_clipper.py` (фаза 2)
- [x] `gsi.py`: `GsiServer` (ThreadingHTTPServer → queue), `GsiTracker` (пакет → события match/kill/assist, базовый пакет, реконнект, демо)
- [x] `series.py`: `SeriesDetector` с инъекцией времени (окно 15, хвост 10/15, закрытие по max(окно, хвост), flush)
- [x] `naming.py`: папка матча, имя клипа, отрицательное время, `unique_path`, `MatchLog` (match.json)
- [x] `cut.py`: `clip_bounds` (усечение по буферу), `ffmpeg_cmd`, `probe_duration`, `cut_clip` (удаление/`_raw`), smoke на реальном ffmpeg
- [x] `dota_paths.py`: поиск Dota через реестр Steam + `libraryfolders.vdf`, `install_cfg` (идемпотентно)
- [x] Ревью python-reviewer + code-reviewer, исправлены HIGH (baseline при рестарте бот-игры, Content-Length, utf-8)

## Фаза 2 — Интеграция с OBS (код готов 2026-09-10, живая загрузка в OBS — ждёт пользователя)
- [x] `killclipper/clipper.py`: оркестрация (пакеты → серии → save → SAVED → резка в рабочем потоке), очередь серий, таймаут 30 с, ожидание буфера, папка матча фиксируется в момент серии — 13 тестов
- [x] `obs_dota_kill_clipper.py`: таймер 100 мс, событие REPLAY_BUFFER_SAVED, старт буфера после загрузки, `RecRB/RecRBTime/RecRBSize` в профиле, панель настроек, кнопка GSI, статус, лог в файл + Script Log; smoke на stub-`obspython` (4 теста)
- [x] Ревью python-reviewer + code-reviewer: HIGH исправлены (папка матча при смене матча во время сохранения, guard в event callback, ожидание буфера вместо потери серии)
- [x] Загрузка в реальном OBS 32.2 под Python 3.12 без traceback, панель настроек и статус работают (2026-09-10 01:35)

## Фаза 3 — Проверка
- [x] Юнит-тесты (pytest): 73 зелёных на 3.14, компиляция/импорт на 3.12
- [x] Прогон с ботами (techies, 2026-09-10 01:38): 15 событий → 12 клипов, все `ok`, длины 20.04–37.7 с, серии 2 kills / kill+assist, отрицательное время, `match.json` заполнен
- [x] Ревью (code-reviewer + python-reviewer) после фаз 1 и 2, CRITICAL/HIGH исправлены

## Фаза 4 — Установка и документация
- [x] `install.ps1`: создаёт `C:\Highlights`, ставит GSI-cfg с токеном, печатает шаги для OBS (проверен 2026-09-10; UTF-8 BOM обязателен для PS 5.1)
- [x] README: установка, настройка OBS (буфер, Python), как работает, устранение неполадок
- [x] Коммиты сделаны; `git push` — только по явной просьбе пользователя

## Фаза 5 — Shorts и YouTube (спека PROJECT.md §10, 2026-09-13)
- [x] Разбор доработки другого ИИ: сбой рабочего потока 13.09 (TypeError, клип завис в статусе «cutting»), CTA без альфы, одновременные загрузки из разных потоков, повтор ежемесячного среза при каждом запуске OBS, текст не на двух языках
- [x] Опрос: CTA с альфой; скрытие < ½ медианы (≥ 8 видео, unlisted); загружать всё; только Techies; хранить оба варианта; ~150 треков; срез при запуске OBS
- [x] `render.py`: слои, CTA в полосах с альфой и циклом, музыка с затуханием, NVENC → libx264, низкий приоритет; реальный рендер клипа 39 с за 30 с
- [x] `seo_dictionary.json`: словари RU/EN (хуки по категориям, вступления, абзацы, призывы, ключевые слова, хештеги, 255 тегов)
- [x] `publishing.py`: название, описание и теги в лимитах YouTube; слоты с запасом 15 мин и освобождением; срез по медиане
- [x] `youtube.py`: вход с проверкой канала, загрузка private + publishAt, плейлисты, срез, unlisted; CLI `login` / `review`
- [x] `pipeline.py`: один рабочий поток, очередь `jobs.json`, повторы, ежемесячный срез с отчётом
- [x] `tools/fetch_music.py`: подборка треков Kevin MacLeod + `credits.json`
- [x] Панель OBS: вертикальный монтаж, CTA, музыка, JSON клиента Google, кнопка «Log in to YouTube», статус очереди
- [ ] Вход в YouTube кнопкой в OBS и первая реальная загрузка — **делает пользователь**
- [ ] Аудит проекта Google для YouTube Data API (без него видео остаются приватными) — **делает пользователь**
