# MEMORY — память задачи dota-kill-clipper

Читать первым при возобновлении работы. Обновлять после каждого значимого шага.

## Суть задачи (одним абзацем)
OBS-плагин для Dota 2: во время записи автоматически сохранять клипы с моими убийствами (10 с до первого убийства,
хвост после последнего, серия = один клип, минимум 20 с, ждать 5 с после последнего убийства серии), раскладывать по папкам
матчей с именами вида `[00.23.37-00.23.57] 1 kill (KDA 3-1-2).mp4`. Корневую папку задаёт пользователь в интерфейсе плагина.
События берём из Dota 2 GSI (рост `player.kills`).

## Принятые решения
| Дата | Решение | Почему |
|---|---|---|
| 2026-09-09 | Репозиторий `C:\Claude code projects\dota-kill-clipper`, ветка `main`, локальный (GitHub — по решению пользователя) | Требование R6; push — зона человека |
| 2026-09-09 | Источник убийств — GSI, не console.log | GSI даёт счётчики kills/deaths/assists, matchid, clock_time каждые 0.1–0.5 с; console.log убийств надёжно не пишет |
| 2026-09-09 | Код детектора GSI брать за основу из `dota-helper-app/d2pt/events.py` и `gsi_config.py` | Проверено в бою, там же поиск пути Dota через реестр Steam |
| 2026-09-09 | Cybersecurity-скиллы (818 шт.) установлены как один индексный скилл `cybersecurity-skills`, а не 818 папок | Иначе список скиллов в каждой сессии раздуется |

| 2026-09-09 | Скрипт внутри OBS на Python 3.12; буфер повтора вместо записи; kills + assists; хвост 10 с (одиночное) / 15 с (серия); окно серии 15 с; ffmpeg `-c copy`; папка матча при входе; корень `C:\Highlights`; публичный GitHub | Ответы пользователя на интервью, см. `SCENARIOS.md` §3 и `PROJECT.md` §2 |

| 2026-09-09 | Резка без запаса: `-ss` до `-i` с `-c copy` сам отступает к предыдущему ключевому кадру (≤2 с раньше), конец по `-to` точный | Запас ±2 с из Q6 получается бесплатно; имя клипа считается по clock_time без запаса |
| 2026-09-09 | Длительность файла буфера берём через `ffprobe` (рядом с ffmpeg), `cut_clip` синхронный, в OBS запускается в рабочем потоке, результат читается на тике | Парсить mp4 руками ради `mvhd` — риск с фрагментированным mp4; блокировать главный поток OBS нельзя |
| 2026-09-09 | Токен GSI хранится в `%LOCALAPPDATA%\dota-kill-clipper	oken.txt` (генерируется при первой установке cfg), сервер отвергает пакеты с чужим токеном (403) | Простой файл, не тащить секрет в настройки сцены OBS |
| 2026-09-09 | Бот-игра/демо (`matchid`=0): новый «матч» при каждом переходе из неактивного состояния (меню, hero selection, post game) в активное; счётчики при этом — базовые | Иначе две бот-игры подряд попали бы в одну папку, а сброс kills терял бы события (ревью) |
| 2026-09-09 | `clip_bounds`: `truncated=True` и при усечении хвоста (конец за пределами файла), не только начала | Спека §4 требует предупреждение в лог, когда бой не влез в буфер |

| 2026-09-10 | Оркестрация вынесена в `killclipper/clipper.py` с инъекцией `buffer`/`cut`/`spawn`; OBS-скрипт — тонкая обвязка. Серия хранит папку и `match.json` того матча, в котором случилась | Состояние «серия → save → SAVED → резка» тестируется без OBS; клип не уезжает в папку следующего матча |
| 2026-09-10 | Буфер неактивен → серия ждёт (старт буфера раз в 5 с), сбрасывается через 60 с после конца серии | Не терять клипы при кратком перезапуске буфера; старше буфера всё равно нечего резать |
| 2026-09-10 | Профиль OBS: скрипт выставляет `RecRB=true`, `RecRBTime≥120`, `RecRBSize≥2048` в секции по `Output/Mode` (AdvOut/SimpleOutput) и просит перезапуск OBS | Из скрипта пересоздать output нельзя; на 2026-09-10 в профиле уже RecRB=true, 120 с, 512 МБ |

## Открытые вопросы
- **Фаза 3 ждёт пользователя**: добавить `obs_dota_kill_clipper.py` в OBS (Tools → Scripts, Python 3.12), затем демо/боты. Правка `user.ini`/scene-json OBS из сессии заблокирована классификатором (и это зона человека).
- Спека утверждена 2026-09-09. Реализация идёт в отдельной сессии: точка входа `docs/START_PROMPT.md` / команда `/implement`.
- Проверить на практике, что OBS 32.2.2 грузит Python 3.12 из `%LOCALAPPDATA%\Python\pythoncore-3.12-64` (первый шаг фазы 2).

## Факты об окружении (проверены 2026-09-09)
- OBS 32.2.2; obs-websocket 5.7.4 встроен, сервер выключен, порт 4455, auth required.
- Профиль «Безымянный»: Advanced, `RecFilePath=D:/Material/Obs записи`, `RecFormat2=mp4`, NVENC h264 tex, 2560×1440,
  `RecSplitFileType=Time`, `RecSplitFileTime=15`, replay buffer выкл (`RecRBTime=20`, `RecRBSize=512`), `FilenameFormatting=%CCYY-%MM-%DD %hh-%mm-%ss`.
- OBS-скриптинг: `data/obs-scripting/64bit/_obspython.pyd` линкуется с `python3.dll` (stable ABI); в документации 32.2.2 заявлено 3.6–3.12;
  Python ставится через Python install manager (`py install <ver>` → `%LOCALAPPDATA%\Python\pythoncore-<ver>-64`, `python` = 3.14 по умолчанию).
  Для OBS поставлен 3.12 рядом (2026-09-09), PATH и дефолт не тронуты. Путь для OBS → Scripts → Python Settings: `%LOCALAPPDATA%\Python\pythoncore-3.12-64`.
- ffmpeg `C:\ffmpeg\bin\ffmpeg.exe` в PATH. Git, gh (Betreazen, логин есть).
- Dota 2: `C:\Program Files (x86)\Steam\steamapps\common\dota 2 beta`; GSI-конфиги: helper (3210, throttle 0.5, только provider+map),
  d2pt (53000, throttle 0.1, player/hero/events и т.д., с токеном). Launch options уже `-console -condebug -conclearlog`.
- Установленные скиллы Claude Code (2026-09-09): superpowers (14), caveman (22, без hooks — плагин-система не задействована),
  humanizer, karpathy-guidelines, tldraw, cybersecurity-skills (индекс). Уже были: ECC 2.2.1, seo, ponytail, spec-pilot, graphify, grill-me.
  В `~/.claude/skills/generated` осталась не-скилл папка из caveman (удаление заблокировано политикой) — безвредна.
- В проекте стоят локальные скиллы `.claude/skills/` (116 шт., 2026-09-09): superpowers, caveman, seo, ponytail, ECC (44 из профиля minimal;
  полный ECC — 286 скиллов, ставится по запросу из `My skill for claude code\ECC\skills`), humanizer, karpathy, tldraw, spec-pilot,
  grill-me/grilling, graphify, cybersecurity-skills. Папка в `.gitignore` — чужие лицензии, в публичный репозиторий не попадает.
- `claude` CLI не в PATH — установка плагинов через marketplace недоступна из сессии, скиллы скопированы папками.

## Грабли реализации
- `install.ps1` для Windows PowerShell 5.1 обязан быть UTF-8 **с BOM**, иначе кириллица ломает парсер.
- Правка файлов конфигурации OBS (`%APPDATA%\obs-studio\user.ini`, scene json) из сессии запрещена классификатором — регистрация скрипта только руками через GUI.
- Heredoc в Bash-инструменте ломается на Python-строках с `'` и `%` — тесты писать через Write.
- В 3.12 для OBS нет pytest; проверять там `python.exe -c "import killclipper.*"` (тесты гоняются на 3.14).
- `ffprobe`/`ffmpeg` из OBS запускать с `CREATE_NO_WINDOW`, иначе мигают консольные окна.

## Журнал
- 2026-09-09 23:13 — постановка задачи (8 пунктов). Разведка, установка скиллов, создание репо и документов, сценарии, вопросы пользователю.
- 2026-09-09 23:34 — ответы на 10 вопросов получены. Спека финализирована, Python 3.12 установлен, репозиторий опубликован. Ждём «да».
- 2026-09-09 23:46 — сессия реализации (`/implement`). Фаза 1: тесты (RED) → 5 модулей `killclipper/` → 57 зелёных, smoke на реальном ffmpeg (20.13 с из 30 с файла), ревью двумя агентами, HIGH исправлены. Коммит фазы 1.
- 2026-09-10 00:30 — Фаза 2: `clipper.py` + `obs_dota_kill_clipper.py`, stub-тест obspython, ревью, 73 теста зелёные. Фаза 4: `install.ps1` (проверен, cfg + токен поставлены), README. Живая проверка в OBS — на пользователе.
